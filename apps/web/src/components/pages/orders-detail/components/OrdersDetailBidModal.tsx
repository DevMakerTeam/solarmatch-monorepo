import { Modal, ModalProps } from "@repo/ui/modal";

const OrdersDetailBidModal = ({ onClose }: Pick<ModalProps, "onClose">) => {
  return <Modal onClose={onClose}>OrdersDetailBidModal</Modal>;
};

export default OrdersDetailBidModal;
